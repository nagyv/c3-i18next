"use strict";
{
    const PLUGIN_CLASS = SDK.Plugins.Fontanus_I18Next;

    PLUGIN_CLASS.Type = class C3I18NextType extends SDK.ITypeBase
    {
        constructor(sdkPlugin, iObjectType)
        {
            super(sdkPlugin, iObjectType);
        }
    };
}